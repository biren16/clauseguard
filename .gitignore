# Python
__pycache__/
*.py[cod]

# Virtual environment
.venv/

# Test cache
.pytest_cache/

# Environment variables / secrets
.env

# Generated local data
*.db
*.sqlite
*.sqlite3

# NOTE: data/knowledge_base.json is intentionally TRACKED. Regenerating it
# requires Gemini embedding credentials (modules/ingest.py) while runtime
# retrieval is local BM25, so the derived artifact ships with the repository
# to keep clean clones runnable without external credentials.
